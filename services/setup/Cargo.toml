[package]
name = "setup"
version.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive", "env"] }
indicatif = "0.18"
inquire = "0.7"
setup_core = { version = "0.1", path = "../../crates/setup" }
